services:
  - type: web
    name: Koraflex-AI-ML
    runtime: python
    buildCommand: pip install --only-binary=:all: -r requirements.txt
    startCommand: python -m uvicorn app.main:app --host 0.0.0.0 --port 8001
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9